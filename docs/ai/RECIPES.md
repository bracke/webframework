# Recipes

Add an action by declaring a handler, registering it in the dispatcher setup,
adding a `data-wf-click` or `data-wf-submit` attribute in rendered HTML, and
returning explicit patches from the handler.
