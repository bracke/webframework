--  Top-level package for the example application.
--  This package intentionally stays minimal and only owns project-wide
--  declarations that do not depend on framework internals.
package App is
   pragma Pure;
end App;
